services:
  lecrown-properties-site:
    image: ${LECROWNPROPERTIES_IMAGE:?LECROWNPROPERTIES_IMAGE is required}
    ports:
      - "127.0.0.1:18033:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/ >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
